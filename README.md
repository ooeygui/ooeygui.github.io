# OoeyGUI GitHub Pages

This repository hosts the Jekyll site for GitHub Pages.

## Local development

1. Install Ruby 3.1.
2. Install bundler and dependencies:
   - `gem install bundler`
   - `bundle install`
3. Run the site:
   - `bundle exec jekyll serve`

The site will be available at http://localhost:4000.
